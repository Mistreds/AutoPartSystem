<Window x:Class="AutoPartSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoPartSystem"
        RenderOptions.BitmapScalingMode="HighQuality"
        xmlns:ViewModel="clr-namespace:AutoPartSystem.ViewModel"
        d:DataContext="{d:DesignInstance Type=ViewModel:MainViewModel}"
        mc:Ignorable="d"
        Background="White"
        Title="MainWindow" Height="650" Width="1200">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.LightBlue.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Button" x:Key="MenuButton" BasedOn="{StaticResource  MaterialDesignFlatButton}">
                <Setter Property="Foreground" Value="White"></Setter>
                <Setter Property="BorderThickness" Value="0,0,0,2"></Setter>
                <Setter Property="BorderBrush" Value="Transparent"></Setter>
                <Setter Property="Width" Value="150"></Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="#014a71"></Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="StackPanel" x:Key="NavStackPanel">
                <Setter Property="Background" Value="#2ea2e2"></Setter>
            </Style>
        </ResourceDictionary>
     
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Файл">
                <MenuItem Header="Добавить товар"></MenuItem>
                <MenuItem Header="Закрыть"></MenuItem>
            </MenuItem>
            <MenuItem Header="Таблицы">
                <MenuItem Header="Таблица завсклада"></MenuItem>
            </MenuItem>
            <MenuItem Header="Администратор">
                <MenuItem Header="Сотрудники"></MenuItem>
                <MenuItem Header="Марки и модели"></MenuItem>
            </MenuItem>
        </Menu>
        <!--<StackPanel Style="{StaticResource NavStackPanel}" Orientation="Horizontal">
            <Button Style="{StaticResource MenuButton}" Command="{Binding OpenPage}" CommandParameter="0" Height="48" Content="Окно завсклада" ></Button>
            <Button Style="{StaticResource MenuButton}" Command="{Binding OpenPage}" CommandParameter="1" Height="48" Content="Администратор" ></Button>
        </StackPanel>-->
        <ContentControl Grid.Row="1" Content="{Binding MainControl}"></ContentControl>
        <StatusBar Grid.Row="2"></StatusBar>
    </Grid>
</Window>
